
* Raw data
global raw_ard "" 
global raw_abs ""

* Project folders
global project "" // path to project folder
global work "${project}/Working"
global data "${project}/Working/data"
global tables "${project}/Working/tables"
global syntax "${project}/Syntax/ard_abs"
global plots "${project}/Working/plots"

* ado-file library in SDS
adopath + R:\ADO_Files\  

